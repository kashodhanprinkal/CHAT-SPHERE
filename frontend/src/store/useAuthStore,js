import { create } from "zustand"

export const useAuthStore = create ((set)=>({
    authUser : {name: "prinkal", _id:"0804"},

    isLoggedIn:false,
    
    login :()=>{
        console.log("we just logged in");
        set({isLoggedIn:true})
    }
}))